---
handoff_protocol: "session-handoff-card/v1.3"
handoff_id: "HOF-DEMO-002"
created_at: "2026-07-19T15:00:00-04:00"
updated_at: "2026-07-19T15:00:00-04:00"
status: "HANDOFF_READY"
history_coverage: "FULL"
language: "zh-CN"
profile: "VERIFIED"
delivery_mode: "repo"
evidence_mode: "mixed"
project_root: '{{PROJECT_ROOT}}'
card_path: '{{CARD_PATH}}'
source_session: 'demo-long-session'
target_models: "any"
---

# 会话交接卡

## 1. 当前目标与边界

- 当前目标：交付中文优先、模型中立、能完整重建长历史并严格校验的 skill-only 插件仓库。
- 当前状态：示例历史已精确分成 4 块并生成 HANDOFF_READY/FULL 交接卡；等待新会话只读复核。
- 验收标准：
  - [x] 全部已提供历史得到连续字符覆盖。
  - [x] 失效要求、失败路线、权限和证据均未丢失。
  - [x] 严格结构与路径校验通过。
- 失效要求：英文优先、自动安装依赖、联网摘要和“不记录失败”均已失效或被拒绝。
- 用户边界与偏好：中文优先；不创建远程仓库、不推送、不安装依赖、不替用户选择许可证；以实际文件和校验为证。

## 2. 已核验证据与现状

| ID | 证明内容或用途 | 来源 | 核验方法或结果 | 状态 | 最后核验时间 |
| --- | --- | --- | --- | --- | --- |
| E1 | 完整示例会话和要求覆盖关系 | file:{{CONVERSATION_PATH}} | 全文与 3 个分块对应字符区间逐一一致 | VERIFIED | 2026-07-19T15:00:00-04:00 |
| E2 | 分块索引连续且覆盖首尾 | file:{{HISTORY_INDEX}} | verify_history.py 返回精确覆盖 | VERIFIED | 2026-07-19T15:00:00-04:00 |
| E3 | v1.3 Skill 和校验器存在 | file:{{SKILL_PATH}} | 文件存在，仓库测试和官方 Skill 校验通过 | VERIFIED | 2026-07-19T15:00:00-04:00 |

- 已完成工作：完整分块并核验示例历史，合并覆盖关系，生成并严格校验核心交接卡。
- 失败、阻塞与未决：联网摘要超时且禁止重试；许可证、维护者身份和 GitHub URL 尚未决定。

## 3. 唯一下一步

- 下一动作：让一个没有旧会话记忆的新会话先只读本卡并复核 E2–E3，报告 `VERIFIED`、`STALE` 或 `BLOCKED`；仅在冲突时升级读取 E1。
- 预期输出：中文接手摘要，准确列出当前目标、失效要求、证据状态和停止条件。
- 验证方式：不得恢复英文优先、自动安装、联网摘要或“不记录失败”，不得新增第二个动作。
- 停止条件：必要证据不可访问、索引不连续、证据变化或用户改变范围时停止。
- 后续候选（非授权）：选择许可证；创建 GitHub Release（均需用户另行授权）。

## 4. 接手说明

- 历史来源与覆盖：`conversation.md` 的消息 001–016 已全部按顺序处理；FULL 只表示全部已提供历史得到覆盖。
- 上下文缺口与影响：无法证明宿主在导出前未截断更早消息。
- 最小接手附件：{{CARD_PATH}}、{{HISTORY_INDEX}}、{{SKILL_PATH}}；{{CONVERSATION_PATH}} 仅在覆盖审计或冲突时提供。
- 路径映射或仓库定位：同机使用绝对路径；跨机器按仓库根目录映射相对路径。
- 新会话首条提示词：请先完整读取交接卡并复核 E2–E3，用中文报告接手分类、失效要求、唯一下一动作和停止条件；仅在冲突时读取 E1，不要立即扩大任务。
- 无法访问原环境时的降级方案：请求卡片和关键证据附件；无法核验的状态标为 UNVERIFIED，影响下一动作时设为 BLOCKED。
